<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/loginActivity">

    <!-- ── Auth ── -->
    <activity
        android:id="@+id/loginActivity"
        android:name="com.mobile.madfya.Login"
        android:label="Login" />

    <!-- ── Resident ── -->
    <activity
        android:id="@+id/dashboardActivity"
        android:name="com.mobile.madfya.Dashboard"
        android:label="Dashboard" />

    <activity
        android:id="@+id/communityActivity"
        android:name="com.mobile.madfya.Community"
        android:label="Community" />

    <activity
        android:id="@+id/statusActivity"
        android:name="com.mobile.madfya.Status"
        android:label="Status" />

    <activity
        android:id="@+id/alertsActivity"
        android:name="com.mobile.madfya.Alerts"
        android:label="Alerts" />

    <activity
        android:id="@+id/userProfileActivity"
        android:name="com.mobile.madfya.UserProfile"
        android:label="Profile" />

    <!-- ── Admin / Maintenance ── -->
    <activity
        android:id="@+id/adminDashboardActivity"
        android:name="com.mobile.madfya.AdminMain"
        android:label="Admin Dashboard" />

</navigation>